════════════════════════════════════════════════════════════════════════
  PS5 FFPFSC PRO — CHANGELOG
════════════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────────────
  v1.4.0  (current)   —   fork by ufukasia
────────────────────────────────────────────────────────────────────────

Fork of KINGDKAK/PS5-FFPFSC-PRO. Everything below is what this fork adds
on top of upstream v1.3.0; the compression backend itself is unchanged.

FLEXIBLE LAYOUT

  • The window now opens to fit the usable desktop instead of a fixed
    1400x960, and honours the Windows display scaling factor. At 125% on
    a 1536x864 laptop the old build asked for 1750x1200 and pushed the
    CPU-core slider and other controls off the screen entirely
  • The three content columns (queue, progress, details) sit behind
    draggable sashes — each one can be widened or narrowed freely
  • Every column is independently scrollable, so no control is stranded
    below the window edge however small the window gets
  • Labels re-wrap live while a column is resized (debounced, so the
    resize cannot feed back into itself)
  • Minimum window size lowered from 1100x760 to fit small displays
  • New "Reset Layout" button restores the default pane sizes after a
    sash has been dragged all the way to an edge
  • Queue list gained a horizontal scrollbar — long entries were being
    cut off at the panel edge — and the mouse wheel now scrolls the list
    under the pointer instead of the column behind it

TURKISH / ENGLISH INTERFACE

  • New EN / TR button in the header switches the whole interface live
  • The chosen language is saved and applied before the UI is built
  • Runtime output (backend log lines, error details, changelog, FAQ)
    deliberately stays English so logs remain shareable for support

SKIP ALREADY-COMPRESSED GAMES

  • New option "Skip games that are already compressed" (on by default,
    saved to settings.json)
  • Finished games are drained from the front of the queue before a
    batch starts, so a batch walks straight past them
  • The backend enforces the same rule with a new --skip-existing flag,
    which also covers --batch runs the GUI cannot pre-check
  • Both the old TITLEID.ffpfsc and the new TITLEID-Game Name.ffpfsc
    layouts are recognised, so games built by earlier versions count

OUTPUT NAMED AFTER THE GAME

  • Output files are now named TITLEID-Game Name.ffpfsc instead of
    TITLEID.ffpfsc — the name comes from sce_sys/param.json, falling
    back to the source folder name
  • Characters Windows rejects in a filename are replaced rather than
    dropped ("NieR:Automata" -> "NieR Automata"), the name is trimmed
    to a sane length, and a title ID already present in the source name
    is not repeated ("PPSA03974-PPSA03974 Cyberpunk 2077")

FIXED

  • UnicodeEncodeError could kill an otherwise finished build on
    Turkish (cp1254) and other non-UTF-8 Windows locales: when stdout
    is a pipe Python falls back to the ANSI codepage and cannot encode
    the icons mkpfs prints. stdout/stderr and every child process are
    forced to UTF-8 now, and log output degrades instead of raising
  • exFAT output drives were reported as having a 4 GB per-file limit.
    Only FAT32/FAT has that cap — exFAT exists precisely to lift it, so
    it is a valid output target and is no longer flagged
  • Free space is now checked against the rule mkpfs actually applies —
    free space >= the full UNCOMPRESSED source size — before the run
    starts, instead of failing after the setup work is already done
  • The Drive Space Diagnostics dialog blocks a run that cannot succeed
    (FAT32 cap or too little free space) instead of auto-proceeding
    into a certain failure, and shows what is missing and by how much
  • mkpfs failure hints no longer blame the output filesystem for
    unrelated failures; the reported cause is the one that holds
  • mkpfs "does not have enough space" / "not enough free space" output
    is now recognised and explained instead of falling through to the
    generic error
  • Option checkboxes that map to settings.json (skip existing,
    auto-clear temp) are saved the moment they are toggled
  • The updater now tracks this fork's releases, and a repository with
    no published release yet reports "up to date" instead of an error
  • Fixed "'Text' object is not callable" poll loop crash caused by
    local variable shadowing the global translation function t()

TÜRKÇE ÖZET

  • Esnek arayüz: pencere ekrana ve ölçeklemeye göre açılıyor, üç sütun
    sürüklenerek genişletilebiliyor, her sütun ayrı kaydırılıyor,
    "Reset Layout" ile varsayılan düzene dönülüyor
  • Arayüz Türkçe / İngilizce arasında anında değiştirilebiliyor
  • Zaten sıkıştırılmış oyunlar atlanıyor (kuyrukta ve arka uçta)
  • Çıktı dosyası oyun adıyla: OYUNKODU-Oyun Adı.ffpfsc
  • Türkçe Windows'ta derlemeyi öldüren UTF-8 hatası, exFAT'in yanlışlıkla
    4 GB sınırlı sayılması, boş alan denetiminin yanlış hesaplanması,
    _poll döngüsündeki 'Text' object is not callable hatası ve bir dizi
    başka hata düzeltildi

────────────────────────────────────────────────────────────────────────
  v1.3.0
────────────────────────────────────────────────────────────────────────

MkPFS 0.0.9 — SINGLE-PASS PIPELINE UPGRADE

  • pack folder now streams directly to a final exFAT-wrapped .ffpfsc
    in a single pass — no intermediate pfs_image.dat temp file needed,
    requiring ~50% less working temp space for folder sources
  • Compression default raised from 7 → 9 for better output size
  • AMPR index (ampr_emu.index) is now built automatically by mkpfs
    when fakelib/libSceAmpr.sprx is present — redundant app-side index
    builder removed
  • Stage display updated to match new single-pass pipeline:
      Scan → Read → Build → Verify → Cleanup
    ("Build" replaces the old Temp PFS + Compress + Write stages)
  • Stage weights rebalanced: Build Image gets 82% of the bar
    (accounts for the combined pack + compress + write work)

NEW FEATURES

  COMMUNITY COMPATIBILITY
  • Live Google Sheet integration — fetches 5,000+ community game
    reports directly inside the app (Compatibility tab)
  • Search bar + status filter (Working / Partial / Not Working /
    Not Tested) on the compatibility list
  • Vote-based submission — one bad report cannot override the
    community majority
  • Duplicate submit protection — resubmitting the same game
    replaces the existing entry instead of stacking rows

  WHAT'S NEW DIALOG + FEATURE TOUR
  • First-run dialog after every update shows all new features with
    descriptions — skippable, re-launchable at any time
  • "Take a Feature Tour" — interactive arrow callouts pointing to
    new UI elements one at a time
  • Re-launchable from Settings → About and the top of Help / FAQ
    (buttons always visible in a fixed header, not buried in scroll)

  APR / AMPR GAME SUPPORT (fully overhauled)
  • Checkbox removed — detection and setup are now fully automatic
  • Auto-detects APR titles via playgo-chunk.dat in sce_sys/
  • Before compression, if not auto-detected, app asks:
    "Is this an APR / AMPR title?" with a clear list of what will
    happen if Yes is selected
  • If AMPR emu folder is not set in Settings, a second dialog
    prompts the user to pick the folder right then (Browse button
    included) — saves the path to Settings automatically
  • fakelib/ folder created inside the game directory and
    libSceAmpr.sprx + libScePlayGo.sprx injected before compression
  • AMPR index (ampr_emu.index) built automatically after injection
    using the AMPRIDX3 binary format with FNV1a64 hashing and an
    open-addressed hash table — covers all /app0 paths including
    the injected fakelib files
  • APR handling runs in both single-game and batch auto-advance
    mode — every game in a queue gets the same prompts

  HELP / FAQ TAB
  • Full FAQ covering common compression errors, OOM, AMPR setup,
    ShadowMount mounting, and archive input
  • YouTube tutorial link (KINGDKAK channel)
  • Tour buttons pinned to a fixed header at the top — always
    visible regardless of scroll position

  SETTINGS
  • What's New and Take a Feature Tour buttons in Settings → About
  • AMPR Emu Folder setting added to Folders section

  COMPRESSION PRESETS
  • One-click profiles: Fast / Balanced / Max Compression / Low RAM
    — sets compression level, CPU cores, and block size together

  LIVE RAM METER
  • Available RAM shown in real time in the log header
  • Updates every 2 seconds during compression

  AUTO UPDATE CHECK
  • Silently checks for a new version 3 seconds after the app opens
  • Only notifies if a newer version is available

  OTHER
  • Windows toast notification when compression finishes
  • Auto-retry on out-of-memory — drops CPU count by 1 and retries
    the failed game automatically
  • Block size selector: auto, auto-fit, 16384, 32768, 65536
  • Per-game output subfolder option (output/GameName/)
  • Multi-image queue — drag in multiple .exfat / .ffpkg images
  • Compression ratio in completion log (original → compressed size
    and exact % saved)

FIXED

  PROGRESS BAR
  • Overall % frozen at 97% for large games — root cause: stage
    order in _STAGE_ORDER was wrong (Verify was after Compressing
    and Writing, so the no-backwards guard blocked all transitions
    after verify finished)
  • Correct pipeline order now enforced:
      Scanning → Reading → Creating Temp PFS → Verifying Output
      → Compressing (outer MkPFS) → Writing Final Image → Cleaning Up
  • Outer MkPFS compression now receives 40% of the overall bar
    (was 0% — it was undetectable, progress sat at 97% the whole time)
  • "outer container" and MkPFS log lines now correctly map to
    Compressing stage instead of Writing Final Image
  • Stage weights rebalanced for accurate ETA on large games:
      Scanning 0–5%  |  Reading 5–15%  |  Temp PFS 15–38%
      Verify 38–48%  |  Compressing 48–88%  |  Writing 88–97%
      Cleaning Up 97–100%

  APR / AMPR STABILITY
  • [ERROR] _poll crash — GameItem objects created via from_exfat()
    or reconstructed from session history were missing the ampr_emu
    attribute; added class-level default (ampr_emu = False) and
    changed all read sites to use getattr() defensively
  • APR ask dialog now fires in batch auto-advance mode — previously
    _batch_auto_start() called CLIWorker directly, bypassing start()
    and the APR check entirely

  OTHER
  • Python < 3.10 crash (str | None union type hint syntax)
  • Unicode arrow crash on cp1252 Windows consoles
  • Multiple game folders false positive — nested sce_sys / eboot.bin
    no longer counted as a second game
  • App crash on startup when a saved temp/output folder path no
    longer exists on disk
  • OOM error message now correctly lists "Uncheck Verify Output"
    as step 1
  • CPU cores slider uncapped — now goes up to 16 cores

────────────────────────────────────────────────────────────────────────
  v1.2.2
────────────────────────────────────────────────────────────────────────

STAGE DISPLAY OVERHAUL
  • Fixed stages permanently stuck on "Scanning Files" — early
    backend path strings were triggering "Creating Temp PFS" before
    scanning began, locking the no-backwards guard
  • Fixed "write" progress bars not advancing Temp PFS % — backend
    labels include trailing speed/ETA; switched to first-word matching
  • Fixed "Verifying Output" firing at startup from the MkPFS
    "post-build verify is disabled" info message
  • Added auto-advance: Compressing 100% now pushes display forward
    to "Writing Final Image" automatically
  • Stage cascade snap — when a later stage begins, all earlier
    stages are forced to 100% so breadcrumbs never show stale %

CRITICAL BUG FIX — stdout / process handling
  • proc.wait() was inside the stdout reading loop — blocked on the
    first line until the process finished; fixed to consume all
    stdout before calling wait()

RAW LOG FIXES
  • Log file flushed to disk every 30 s during the run (previously
    only written at job completion)
  • Log flood fixed — progress entries only written at 5% bucket
    boundaries and at 100%

ARCHIVE EXTRACTION
  • RAR extraction fixed — 7-Zip detected at default install path
    without needing it on PATH
  • _find_root improved — BFS search locates game folder via
    sce_sys/param.json inside extracted content

GAME NAME FIX
  • Games showing as parent folder name fixed
  • guess_game_name() priority: (1) param.json localised title,
    (2) strip -app/_app suffix, (3) never use parent folder name

COMMUNITY COMPATIBILITY
  • Duplicate reports fixed — resubmitting replaces existing entry
  • Startup re-test reminder 4 s after launch for untested games

────────────────────────────────────────────────────────────────────────
  v1.2.1
────────────────────────────────────────────────────────────────────────

  • Resizable log pane — drag the divider between the main area
    and the log panel
  • Smart auto-scroll — scrolling up in the log pauses auto-scroll;
    scrolling back to the bottom resumes it
  • Compatibility report status picker (Working / Partial /
    Not Working / Not Tested)
  • Dark / light theme toggle in Settings
  • Queue drag-and-drop reordering

────────────────────────────────────────────────────────────────────────
  v1.2.0
────────────────────────────────────────────────────────────────────────

  • Archive input — ADD ARCHIVE accepts .zip, .rar, .7z; extracts
    to temp and queues the game automatically
  • Multi-game batch auto-advance — queue multiple games, press
    START once, they all compress sequentially
  • Batch counter label (Game X/N | done/failed) in the header
  • Batch complete summary popup (total / successful / failed)
  • Auto-clear temp after success option
  • Post-flight output validator — checks file exists, correct
    extension, non-zero size
  • Summary dialog with scrollable report and Copy Result button
  • ShadowMount help card in Status & Stats tab
  • Smart error messages — scans log for known keywords and shows
    plain-English fix hints
  • Game structure validation warns when sce_sys / param.json /
    eboot.bin are missing
  • Two-pass compression workflow
  • Block size selector
  • Verify output option
  • Compatibility list with CSV export
  • Drag & drop game folders onto queue
  • open_path() helper for cross-platform file/folder opening

────────────────────────────────────────────────────────────────────────
  v1.1
────────────────────────────────────────────────────────────────────────

  • DPI scaling removed; layout redesigned to 2-column + bottom tabs
    (eliminates right-column cutoff at 200% DPI)
  • Light-mode rendering fixed — all colour constants converted to
    CTk native (light, dark) tuples
  • START / CANCEL buttons moved to header (always visible)
  • Bottom tabs: Logs | Status & Stats | Recent Compressions |
    Statistics
  • Drive / filesystem type detection (NTFS check for hardlinks)
  • Drag-and-drop support via tkinterdnd2

────────────────────────────────────────────────────────────────────────
  v1.0
────────────────────────────────────────────────────────────────────────

  • Initial release
  • Bizkut backend integration (mkpfs / ffpfsc)
  • Single-game queue with progress bars, stage display, and log tab
  • Settings dialog with output / temp folder configuration
  • First-run setup wizard

════════════════════════════════════════════════════════════════════════
